import 'package:flutter/material.dart';

abstract class AppColors {
  // Backgrounds
  static const background = Color(0xFF0F0E0E);
  static const surface = Color(0xFF1A1919);

  // Text
  static const textPrimary = Color(0xFFF3E9E9);
  static const textSecondary = Color(0xFFDED5D5);
  static const textMuted = Color(0xFF968D8D);

  // Brand
  static const primaryRed = Color(0xFFCB2C2C);
  static const primaryRedDark = Color(0xFF8C2626);

  // States
  static const disabled = Color(0xFF968D8D);
}
